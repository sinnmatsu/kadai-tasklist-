<h1>メッセージ一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id,task %> :<%= task.status %> &gt; <%= task.content %> </li>
    <!-- taskでそのインスタンスのidに適用するリソースURLに飛ぶ-->
    <!-- link_toはa要素を表している-->
  <% end %>
</ul>

<%= link_to '新規メッセージの投稿' , new_task_path %>